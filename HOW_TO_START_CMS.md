# تشغيل نظام إدارة المحتوى (CMS)

لقد قمنا بتجهيز نظام إدارة محتوى كامل لموقعك (CMS) يسمح لك بـ:
1. تسجيل الدخول كمالك للموقع.
2. إضافة مشاريع جديدة مع صور.
3. تعديل بيانات الموقع (قريباً).
4. عرض المشاريع تلقائياً في الصفحة الرئيسية وصفحة أعمالنا.

## خطوات التشغيل

### 1. تثبيت البرامج اللازمة (مرة واحدة فقط)
يجب أن يكون لديك **Node.js** مثبتاً على جهازك. إذا لم تقم بذلك:
1. افتح التيرمنال (Terminal).
2. اكتب الأمر التالي لتثبيت المكتبات المطلوبة:
   ```bash
   npm install
   ```
   (هذا سيقوم بتثبيت `express`, `multer`, `jsonwebtoken` المذكورة في ملف `package.json`).

### 2. تشغيل السيرفر
لكي يعمل الموقع والـ CMS معاً، يجب تشغيل السيرفر المحلي.
1. افتح التيرمنال.
2. اكتب الأمر:
   ```bash
   npm start
   ```
3. ستظهر رسالة: `CMS server running on 3000`.

### 3. الدخول للوحة التحكم
1. افتح المتصفح واذهب إلى: `http://localhost:3000/login.html`
2. بيانات الدخول الافتراضية (كما هي في الكود):
   - **البريد الإلكتروني**: `ramadan.nady1985@gmail.com`
   - **كلمة المرور**: `01099797984`
3. بعد الدخول، يمكنك إضافة مشاريع جديدة وستظهر فوراً في الموقع.

### 4. تصفح الموقع
- الصفحة الرئيسية: `http://localhost:3000/index.html`
- ستجد أن قسم "أحدث أعمالنا" يتم جلبه الآن من قاعدة البيانات (الملفات) وليس ثابتاً في الكود.

## ملاحظات هامة
- الصور التي ترفعها يتم حفظها في مجلد `uploads/projects`.
- بيانات المشاريع يتم حفظها في ملف `data/projects.json`.
- لا تقم بمسح مجلد `data` أو `uploads` حتى لا تفقد بياناتك.
