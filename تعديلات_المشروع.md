# سجل تعديلات مشروع Decorator

كل تعديل يحدث في المشروع يُسجَّل هنا (الملف + وصف التعديل).

---

## 2026-02-15

### لوحة تحكم المالك (CMS)
- **الملف:** `public/owner-dashboard.html`
- **التعديل:** إنشاء لوحة تحكم كاملة للمالك مع تسجيل دخول (JWT)، إضافة مشاريع، إعدادات المحتوى (عنوان الموقع + نص الترحيب)، تبويبان (المشروعات / إعدادات المحتوى).

### الخادم – خدمة الملفات والـ API
- **الملف:** `backend/server.js`
- **التعديل:** إضافة `app.use('/css', ...)` لخدمة مجلد CSS؛ إضافة `app.use('/', express.static('..'))` لخدمة الصفحة الرئيسية من جذر المشروع؛ إضافة مسارات API عامة: `GET /api/content` و `GET /api/projects` لربط الموقع بالـ CMS.

### ربط الموقع بالـ CMS
- **الملف:** `index.html`
- **التعديل:** تحميل المحتوى من `/api/content` (نص الترحيب + عنوان الموقع) وتحميل المشاريع من `/api/projects` وعرضها في قسم «أحدث أعمالنا».
- **الملف:** `portfolio.html`
- **التعديل:** قسم «مشاريعنا الحالية» يُملأ من `/api/projects` مع إمكانية عرض صورة وفيديو للمشروع.

### رفع صور وفيديو للمشاريع
- **الملف:** `package.json`
- **التعديل:** إضافة dependency: `multer`.
- **الملف:** `backend/server.js`
- **التعديل:** إضافة مجلد الرفع، مسار `POST /cms/upload` (صورة + فيديو)، حفظ `imageUrl` و `videoUrl` مع المشروع في `POST /cms/projects`، خدمة `/uploads`.
- **الملف:** `public/owner-dashboard.html`
- **التعديل:** حقلا رفع «صورة المشروع» و«فيديو المشروع» في نموذج إضافة مشروع؛ رفع الملفات ثم حفظ المشروع بالروابط.
- **الملف:** `portfolio.html`
- **التعديل:** عرض صورة وفيديو المشروع من الـ CMS عند وجودهما.

### تنسيق الموبايل وتوحيد الصفحات
- **الملف:** `css/style.css`
- **التعديل:** تنسيق موحد للموبايل (شاشات 1024px، 768px، 480px): قائمة تنقل للموبايل، أزرار لا تقل عن 44px، Hero و Footer متجاوبان، صور/فيديو `max-width: 100%`، حقول إدخال `font-size: 16px` على الموبايل. إضافة `.footer-contact-link` للفوتر.
- **الملف:** `index.html`, `portfolio.html`, `contact.html`, `about.html`, `services.html`, `packages.html`
- **التعديل:** إضافة `viewport-fit=cover` في meta viewport؛ توحيد الهيدر (`header` و `nav` مع الـ classes المناسبة).
- **الملف:** `public/owner-dashboard.html`
- **التعديل:** إضافة media queries للموبايل (768px و 480px) للوحة التحكم.

### ترتيب أدوات التواصل في الصفحة الرئيسية
- **الملف:** `index.html`
- **التعديل:** إعادة ترتيب الفوتر: «روابط سريعة» (بدون تسجيل الدخول)، «أدوات التواصل» بالترتيب: هاتف → واتساب → الموقع → أيقونات وسائل التواصل (واتساب، فيسبوك، انستغرام، تويتر). إزالة رابط تسجيل الدخول من الفوتر.
- **الملف:** `css/style.css`
- **التعديل:** إضافة تنسيق `.footer-contact-link` لروابط أدوات التواصل.

---

*أي تعديل جديد في المشروع سيُضاف تلقائياً إلى هذا الملف.*
